{
  "installCommand": "npm install --prefix peerClient",
  "buildCommand": "npm run build --prefix peerClient",
  "outputDirectory": "peerClient/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
